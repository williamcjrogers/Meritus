import { pgTable, uuid, text, jsonb, timestamp, integer, bigint, boolean, index, uniqueIndex, customType, date, primaryKey, check } from "drizzle-orm/pg-core";
import { sql } from "drizzle-orm";
import { QCS_WORKSPACE_ID } from "@/lib/research/contracts";
const base = () => ({ id: uuid("id").primaryKey().defaultRandom(), workspaceId: uuid("workspace_id").notNull().default(QCS_WORKSPACE_ID), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow() });
const time = (name: string) => timestamp(name, { withTimezone: true });
const vector = customType<{
    data: string;
}>({ dataType: () => "tsvector" });
export const researchRights = pgTable("research_rights", { ...base(), holder: text("holder").notNull(), material: text("material").notNull(), purpose: text("purpose").notNull(), agreementRef: text("agreement_ref").notNull(), agreementHash: text("agreement_hash").notNull(), effectiveAt: time("effective_at").notNull(), expiresAt: time("expires_at"), transferConditions: jsonb("transfer_conditions").notNull(), retentionInstructions: jsonb("retention_instructions").notNull(), withdrawalInstructions: jsonb("withdrawal_instructions").notNull(), useAssessment: text("use_assessment").notNull() });
export const researchSources = pgTable("research_sources", { ...base(), label: text("label").notNull(), provider: text("provider").notNull(), hosts: jsonb("hosts").$type<string[]>().notNull(), accessMethod: text("access_method").notNull(), termsUrl: text("terms_url").notNull(), termsVersion: text("terms_version").notNull(), termsReviewedAt: time("terms_reviewed_at").notNull(), attribution: text("attribution").notNull(), operator: text("operator").notNull(), purpose: text("purpose").notNull(), selection: jsonb("selection").$type<Record<string, unknown>>().notNull().default({}), backfillStart: time("backfill_start").notNull(), configurationError: text("configuration_error"), rightsId: uuid("rights_id").notNull().references(() => researchRights.id), credentialRef: text("credential_ref"), status: text("status").notNull().default("unavailable"), cadenceSeconds: integer("cadence_seconds").notNull(), freshnessSeconds: integer("freshness_seconds").notNull(), lastSuccessAt: time("last_success_at"), nextDueAt: time("next_due_at").notNull().defaultNow(), requestLimit: integer("request_limit").notNull(), windowSeconds: integer("window_seconds").notNull(), dailyRequests: integer("daily_requests").notNull(), dailyTokens: bigint("daily_tokens", { mode: "number" }).notNull(), dailyPence: integer("daily_pence").notNull() }, t => [index("research_sources_updated").on(t.updatedAt), check("research_source_status", sql `${t.status} in ('ready','unavailable','paused')`), check("research_source_limits", sql `${t.cadenceSeconds}>0 and ${t.freshnessSeconds}>0 and ${t.requestLimit}>0 and ${t.windowSeconds}>0 and ${t.dailyRequests}>=0 and ${t.dailyTokens}>=0 and ${t.dailyPence}>=0`)]);
export const researchEntities = pgTable("research_entities", { ...base(), kind: text("kind").notNull(), displayName: text("display_name").notNull(), jurisdiction: text("jurisdiction"), confirmed: boolean("confirmed").notNull().default(false) }, t => [index("research_entities_updated").on(t.updatedAt)]);
export const researchIdentifiers = pgTable("research_identifiers", { ...base(), entityId: uuid("entity_id").notNull().references(() => researchEntities.id), scheme: text("scheme").notNull(), value: text("value").notNull(), sourceId: uuid("source_id").notNull().references(() => researchSources.id), verified: boolean("verified").notNull().default(false) }, t => [uniqueIndex("research_verified_identifier").on(t.workspaceId, t.scheme, t.value).where(sql `verified and scheme in ('uk-company-number','lei','ocid')`)]);
export const researchInvestigations = pgTable("research_investigations", { ...base(), question: text("question").notNull(), scope: jsonb("scope").notNull(), owner: text("owner").notNull(), status: text("status").notNull().default("draft"), budget: jsonb("budget").notNull(), latestCompletedRunId: uuid("latest_completed_run_id") }, t => [index("research_investigations_updated").on(t.updatedAt)]);
export const researchRuns = pgTable("research_runs", { ...base(), investigationId: uuid("investigation_id").references(() => researchInvestigations.id), sourceSelection: jsonb("source_selection").notNull(), status: text("status").notNull().default("queued"), startedAt: time("started_at"), finishedAt: time("finished_at"), versions: jsonb("versions").notNull(), reservedPence: integer("reserved_pence").notNull().default(0), actualPence: integer("actual_pence").notNull().default(0), actualTokens: bigint("actual_tokens", { mode: "number" }).notNull().default(0), coverage: jsonb("coverage").notNull().default({}), errorSummary: text("error_summary") });
export const researchDocuments = pgTable("research_documents", { ...base(), sourceId: uuid("source_id").notNull().references(() => researchSources.id), providerId: text("provider_id").notNull(), currentVersionId: uuid("current_version_id"), canonicalUrl: text("canonical_url").notNull(), status: text("status").notNull().default("available"), availabilityCheckedAt: time("availability_checked_at").notNull() }, t => [uniqueIndex("research_document_identity").on(t.workspaceId, t.sourceId, t.providerId), index("research_documents_updated").on(t.updatedAt)]);
export const researchVersions = pgTable("research_versions", { ...base(), documentId: uuid("document_id").notNull().references(() => researchDocuments.id), hash: text("hash").notNull(), sourceUpdatedAt: time("source_updated_at"), retrievedAt: time("retrieved_at").notNull(), eventAt: time("event_at"), publishedAt: time("published_at"), objectKey: text("object_key"), contentType: text("content_type").notNull(), availability: text("availability").notNull().default("available"), parserVersion: text("parser_version").notNull(), metadata: jsonb("metadata").notNull() }, t => [index("research_version_hash").on(t.documentId, t.hash)]);
export const researchPassages = pgTable("research_passages", { ...base(), versionId: uuid("version_id").notNull().references(() => researchVersions.id), locator: jsonb("locator").notNull(), text: text("text").notNull(), hash: text("hash").notNull(), search: vector("search").generatedAlwaysAs(sql `to_tsvector('english', text)`) }, t => [index("research_passage_search").using("gin", t.search), uniqueIndex("research_passage_identity").on(t.versionId, t.hash)]);
export const researchJobs = pgTable("research_jobs", { ...base(), type: text("type").notNull(), sourceId: uuid("source_id").notNull().references(() => researchSources.id), scopeKey: text("scope_key").notNull(), runId: uuid("run_id").notNull().references(() => researchRuns.id), dedupeKey: text("dedupe_key").notNull().unique(), payload: jsonb("payload").notNull(), status: text("status").notNull().default("queued"), cursor: text("cursor"), attempts: integer("attempts").notNull().default(0), nextAttemptAt: time("next_attempt_at").notNull().defaultNow(), leaseOwner: text("lease_owner"), leaseToken: bigint("lease_token", { mode: "number" }).notNull().default(0), leaseExpiresAt: time("lease_expires_at"), cancelledAt: time("cancelled_at"), errorCode: text("error_code") }, t => [uniqueIndex("research_one_active_scope").on(t.workspaceId, t.sourceId, t.scopeKey).where(sql `status in ('queued','running','retry')`), index("research_jobs_due").on(t.status, t.nextAttemptAt), check("research_lease_nonnegative", sql `${t.leaseToken}>=0`)]);
export const researchCheckpoints = pgTable("research_checkpoints", { ...base(), sourceId: uuid("source_id").notNull().references(() => researchSources.id), scopeKey: text("scope_key").notNull(), cursor: text("cursor"), watermarkAt: time("watermark_at"), revision: bigint("revision", { mode: "number" }).notNull().default(0) }, t => [uniqueIndex("research_checkpoint_scope").on(t.workspaceId, t.sourceId, t.scopeKey)]);
export const researchReviews = pgTable("research_reviews", { ...base(), actor: text("actor").notNull(), action: text("action").notNull(), target: uuid("target").notNull(), reason: text("reason").notNull(), references: jsonb("references").notNull() });
export const researchInvalidations = pgTable("research_invalidations", { ...base(), documentId: uuid("document_id").notNull().references(() => researchDocuments.id), versionId: uuid("version_id").notNull().references(() => researchVersions.id), reason: text("reason").notNull(), status: text("status").notNull().default("pending"), attempts: integer("attempts").notNull().default(0) }, t => [uniqueIndex("research_invalidation_version").on(t.versionId)]);
export const researchStagedObjects = pgTable("research_staged_objects", { ...base(), objectKey: text("object_key").notNull().unique(), sourceId: uuid("source_id").notNull().references(() => researchSources.id), size: bigint("size", { mode: "number" }).notNull(), sha256: text("sha256").notNull(), claimedAt: time("claimed_at") }, t => [index("research_staged_cleanup").on(t.claimedAt, t.createdAt), check("research_object_size", sql `${t.size}>=0`)]);
export const researchBudgetDays = pgTable("research_budget_days", { workspaceId: uuid("workspace_id").notNull().default(QCS_WORKSPACE_ID), sourceId: uuid("source_id").notNull().references(() => researchSources.id), day: date("day").notNull(), requests: bigint("requests", { mode: "number" }).notNull().default(0), tokens: bigint("tokens", { mode: "number" }).notNull().default(0), pence: bigint("pence", { mode: "number" }).notNull().default(0) }, t => [primaryKey({ columns: [t.sourceId, t.day] }), check("research_budget_nonnegative", sql `${t.requests}>=0 and ${t.tokens}>=0 and ${t.pence}>=0`)]);
export const researchRequestReservations = pgTable("research_request_reservations", { ...base(), sourceId: uuid("source_id").notNull().references(() => researchSources.id), runId: uuid("run_id").notNull().references(() => researchRuns.id), reservedAt: time("reserved_at").notNull().defaultNow() }, t => [index("research_request_window").on(t.sourceId, t.reservedAt)]);
export const researchModelReservations = pgTable("research_model_reservations", { ...base(), sourceId: uuid("source_id").notNull().references(() => researchSources.id), runId: uuid("run_id").notNull().references(() => researchRuns.id), day: date("day").notNull(), tokens: bigint("tokens", { mode: "number" }).notNull(), pence: bigint("pence", { mode: "number" }).notNull(), actualTokens: bigint("actual_tokens", { mode: "number" }), actualPence: bigint("actual_pence", { mode: "number" }), status: text("status").notNull().default("reserved"), expiresAt: time("expires_at").notNull().default(sql `now()+interval '120 seconds'`) }, t => [index("research_model_active").on(t.status, t.expiresAt), check("research_model_units", sql `${t.tokens}>=0 and ${t.pence}>=0 and (${t.actualTokens} is null or ${t.actualTokens}>=0) and (${t.actualPence} is null or ${t.actualPence}>=0)`)]);
export const researchLegacyChRequests = pgTable("research_legacy_ch_requests", { ...base(), reservedAt: time("reserved_at").notNull().defaultNow() }, t => [index("research_legacy_ch_window").on(t.reservedAt)]);
export const researchRunDocuments = pgTable("research_run_documents", { ...base(), runId: uuid("run_id").notNull().references(() => researchRuns.id), documentId: uuid("document_id").notNull().references(() => researchDocuments.id), metadata: jsonb("metadata").$type<Record<string, unknown>>().notNull().default({}), versionId: uuid("version_id").notNull().references(() => researchVersions.id) }, t => [uniqueIndex("research_run_version").on(t.runId, t.versionId), index("research_run_document_lookup").on(t.workspaceId, t.runId, t.documentId)]);
